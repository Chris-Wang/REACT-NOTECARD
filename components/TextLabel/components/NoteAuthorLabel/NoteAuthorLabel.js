import styled from "styled-components";

const NoteAuthorLabel = styled.div`
    position: relative;
    box-sizing: border-box;

    padding: 0;
    margin: 0;

    width: 160px;
    height: 36px;

    font-size: 1.4em;
    font-family: New Paris;
    font-weight: normal;
    font-stretch: normal;
    font-style: normal;
    line-height: 1.6;
    letter-spacing: 0.18px;
    text-align: center;
    color: black;

`;

export default NoteAuthorLabel;